import type { Diff } from '../../../utils/json';
declare const strategies: {
    exact(diffs: Diff[]): Diff<unknown>[];
    strict(diffs: Diff[]): Diff<unknown>[];
};
declare const compareSchemas: <T, P>(a: T, b: P, strategy: keyof typeof strategies) => Diff<unknown>[];
export { compareSchemas };
